# Used by `npm run dev`. Not a secret — the local API's address.
# Production builds read VITE_API_URL from the hosting platform instead
# (e.g. https://beytak-api.azurewebsites.net/api).
VITE_API_URL=https://localhost:7137/api
